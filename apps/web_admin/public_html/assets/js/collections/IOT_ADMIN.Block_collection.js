var IOT_ADMIN = IOT_ADMIN || {};

(function () {
    IOT_ADMIN.Block_collection = Backbone.Collection.extend({

        model: IOT_ADMIN.Block_model,

        url: '',

        /**
         * Init
         */
        initialize: function () {

        }
    })
})();

