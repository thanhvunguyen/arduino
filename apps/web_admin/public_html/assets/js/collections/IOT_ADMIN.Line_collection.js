var IOT_ADMIN = IOT_ADMIN || {};

(function () {
    IOT_ADMIN.Line_collection = Backbone.Collection.extend({

        model: IOT_ADMIN.Line_block_current_model,

        /**
         * Init
         */
        initialize: function () {

        }
    })
})();

