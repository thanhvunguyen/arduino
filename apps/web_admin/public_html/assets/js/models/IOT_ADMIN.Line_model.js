var IOT_ADMIN = IOT_ADMIN || {};
(function () {

    IOT_ADMIN.Line_block_current_model = Backbone.Model.extend({

        /**
         * Parse data
         *
         * @param response
         *
         * @returns {*}
         */
        parse: function (response) {

        }
    });

})();